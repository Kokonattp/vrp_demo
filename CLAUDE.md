# VRP Project Instructions

Before changing business logic, routing constraints, workflow behavior, authentication, integrations, or release configuration, read `PROJECT_MEMORY.md` and `PROJECT_HANDOFF.md`.

Preserve unrelated working-tree changes. Keep route source truth separate from fallback/simulated output, and do not claim road-routing, provider, browser, device, or production evidence from source inspection or screenshots alone.

The current priority is Map Truth and Planner Usability Hardening. Keep MiroFish as a future scenario layer; do not use it as a substitute for deterministic VRP optimization or road-routing validation.
